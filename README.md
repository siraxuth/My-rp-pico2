# คู่มือการใช้งาน MyRP Pico2 Library

## 📚 สารบัญ
- [เกี่ยวกับ Library](#เกี่ยวกับ-library)
- [การติดตั้งและเริ่มต้นใช้งาน](#การติดตั้งและเริ่มต้นใช้งาน)
- [อุปกรณ์และฮาร์ดแวร์](#อุปกรณ์และฮาร์ดแวร์)
- [ฟังก์ชันการตั้งค่าเริ่มต้น](#ฟังก์ชันการตั้งค่าเริ่มต้น)
- [ฟังก์ชันเซ็นเซอร์](#ฟังก์ชันเซ็นเซอร์)
- [ฟังก์ชันควบคุมมอเตอร์](#ฟังก์ชันควบคุมมอเตอร์)
- [ฟังก์ชันเดินตามเส้น (Line Tracking)](#ฟังก์ชันเดินตามเส้น-line-tracking)
- [ฟังก์ชันควบคุมด้วย Gyro](#ฟังก์ช canันควบคุมด้วย-gyro)
- [ฟังก์ชันการเลี้ยว](#ฟังก์ชันการเลี้ยว)
- [ฟังก์ชันแขนหุ่นยนต์และ Gripper](#ฟังก์ชันแขนหุ่นยนต์และ-gripper)
- [ฟังก์ชัน Servo](#ฟังก์ชัน-servo)
- [ตัวอย่างโค้ดจากไฟล์ต่างๆ](#ตัวอย่างโค้ดจากไฟล์ต่างๆ)

---

## เกี่ยวกับ Library

**MyRP_Pico2** เป็น Library สำหรับการควบคุมหุ่นยนต์ที่ใช้ Raspberry Pi Pico รองรับการทำงานหลากหลาย:

- ✅ เดินตามเส้นด้วย PID Controller
- ✅ อ่านเซ็นเซอร์อนาล็อกถึง 8 ตัว (A, B, C set)
- ✅ ควบคุมมอเตอร์ 2 ตัว (DC/Coreless)
- ✅ การนำทางด้วย Gyroscope (IMU)
- ✅ ควบคุมแขนกลและ Gripper ด้วย Servo
- ✅ บันทึก/โหลดค่า Calibration ผ่าน EEPROM

**เวอร์ชัน**: 1.0  
**ผู้พัฒนา**: MyRP Team  
**รองรับ**: Raspberry Pi Pico / RP2040

---

## การติดตั้งและเริ่มต้นใช้งาน

### การติดตั้ง Library

1. คัดลอกโฟลเดอร์ `MyRP_Pico2` ไปยัง `Arduino/libraries/`
2. เปิด Arduino IDE และเลือก Board: **Raspberry Pi Pico**
3. Include library ในโค้ด:

```cpp
#include <myrp_pico2.h>
```

### โค้ดเริ่มต้นพื้นฐาน

```cpp
#include <myrp_pico2.h>
#include "CONFIG.h"

int BaseSpeed = 50;

void setup() {
  setup_robot();      // <<<--- เริ่มต้นระบบหุ่นยนต์
  set_motor();        // ตั้งค่ามอเตอร์
  sw();               // รอกดปุ่มเริ่มต้น
}

void loop() {
  // ใส่โค้ดของคุณที่นี่
}
```

---

## อุปกรณ์และฮาร์ดแวร์

### เซ็นเซอร์เส้น

Library รองรับเซ็นเซอร์ 3 ชุด:

| ชุด | จำนวนเซ็นเซอร์ | พอร์ต | การใช้งาน |
|-----|----------------|-------|----------|
| **Sensor A (หน้า)** | 8 ตัว (0-7) | MCP3008 (CS: Pin 17) | เดินตามเส้นหน้า |
| **Sensor B (หลัง)** | 8 ตัว (0-7) | MCP3008 (CS: Pin 13) | เดินตามเส้นหลัง/ถอยหลัง |
| **Sensor C (กลาง)** | 2 ตัว | Analog A0, A1 (Pin 26, 27) | ตรวจจับเส้นกลาง |

### มอเตอร์ DC

```cpp
// พินควบคุมมอเตอร์
#define PWMA 19    // PWM มอเตอร์ซ้าย
#define AIN1 20    // ทิศทางมอเตอร์ซ้าย
#define AIN2 21

#define PWMB 6     // PWM มอเตอร์ขวา
#define BIN1 8     // ทิศทางมอเตอร์ขวา
#define BIN2 7
```

### Gyroscope (IMU)

- ใช้ `my_GYRO1600` ที่ I2C Address: `0x69`
- วัดมุม X, Y, Z (Roll, Pitch, Yaw)
- ใช้สำหรับเดินตรง/หมุนตัวแม่นยำ

### Servo Motors

| Channel | Pin | การใช้งาน |
|---------|-----|----------|
| Servo 0 | GPIO 0 | Gripper ขวา |
| Servo 1 | GPIO 1 | แขนกล (ยก/ลง) |
| Servo 10 | GPIO 10 | Gripper ซ้าย |
| Servo 28 | GPIO 28 | สำรอง |

### Encoder

- Pin: 18, 22 (Left), 12, 11 (Right)
- ใช้วัดระยะทาง/ความเร็ว

### ปุ่มและเสียง

```cpp
pinMode(3, INPUT_PULLUP);  // ปุ่ม SW1
pinMode(2, INPUT_PULLUP);  // ปุ่ม SW2
pinMode(9, OUTPUT);        // Buzzer
```

---

## ฟังก์ชันการตั้งค่าเริ่มต้น

### `setup_robot()`
เริ่มต้นระบบทั้งหมด (Serial, I2C, Gyro, EEPROM)

```cpp
void setup() {
  setup_robot();  // <<< เรียกฟังก์ชันนี้เสมอในตอนเริ่มต้น
}
```

**การทำงาน:**
- ตั้งค่า Serial 9600 baud
- เริ่มต้น I2C, Analog 12-bit
- โหลดค่า Calibration จาก EEPROM
- เริ่มต้น Gyroscope
- ส่งเสียง beep แจ้งพร้อม

### `set_motor()`
ตั้งค่าพารามิเตอร์มอเตอร์และ PID

```cpp
void set_motor() {
  set_Freq("Coreless_Motors");   // "Coreless_Motors" หรือ "DC_Motors"
  distance_scale_fw(1.00);        // ปรับมาตราส่วนระยะทางเดินหน้า
  distance_scale_bw(1.10);        // ปรับมาตราส่วนถอยหลัง
  set_kd_f(1.12);                 // ค่า Kd สำหรับเบรกเดินหน้า
  
  set_slow_motor(20, 20);         // ความเร็วต่ำสุด
  set_turn_center_l(-90, 90);     // กำลังหมุนซ้ายแบบ center
  set_turn_center_r(90, -90);     // กำลังหมุนขวาแบบ center
  set_turn_front_l(-100, 100);    // กำลังหมุนซ้ายแบบ front
  set_turn_front_r(100, -100);    // กำลังหมุนขวาแบบ front
  set_brake_fc(5, 20);            // เบรกเดินหน้า (delay_front, delay_center)
  set_brake_bc(5, 20);            // เบรกถอยหลัง
  set_delay_f(10);                // หน่วงก่อนเลี้ยว
}
```

### `sw()`
รอกดปุ่มเพื่อเริ่ม (แสดงค่าเซ็นเซอร์และรอ calibrate)

```cpp
void setup() {
  setup_robot();
  set_motor();
  sw();  // <<< รอกดปุ่ม SW เพื่อเริ่ม
  // โปรแกรมจะทำงานต่อหลังกดปุ่ม
}
```

**การใช้งาน SW:**
- กด ปุ่ม 3 (SW1): Calibrate Sensor A
- กด ปุ่ม 2 (SW2) นานกว่า 3 วินาที: Calibrate Sensor C
- กด ปุ่ม 2 สั้นๆ: ออกจากโหมด calibrate

---

## ฟังก์ชันเซ็นเซอร์

### อ่านค่าเซ็นเซอร์

#### `read_sensorA(sensor)` - อ่านเซ็นเซอร์หน้า
```cpp
uint16_t value = read_sensorA(3);  // อ่านเซ็นเซอร์หน้าตัวที่ 3 (0-7)
```

#### `read_sensorB(sensor)` - อ่านเซ็นเซอร์หลัง
```cpp
uint16_t value = read_sensorB(5);  // อ่านเซ็นเซอร์หลังตัวที่ 5 (0-7)
```

#### `analogRead(A0)` / `analogRead(A1)` - อ่านเซ็นเซอร์กลาง
```cpp
int valueC0 = analogRead(A0);  // เซ็นเซอร์กลางซ้าย
int valueC1 = analogRead(A1);  // เซ็นเซอร์กลางขวา
```

### อ่านค่ากลาง (Mid-point)

#### `md_sensorA(sensor)` - ค่ากลางเซ็นเซอร์ A
```cpp
int midpoint = md_sensorA(3);  // (Max + Min) / 2
if (read_sensorA(3) < md_sensorA(3)) {
  // เจอเส้นดำ
}
```

#### `md_sensorB(sensor)` / `md_sensorC(sensor)`
```cpp
int mid_B = md_sensorB(5);
int mid_C0 = md_sensorC(0);
int mid_C1 = md_sensorC(1);
```

### Calibration เซ็นเซอร์

#### `get_maxMinA()` - Calibrate เซ็นเซอร์ A
เก็บค่า Max/Min จาก 1000 ตัวอย่างแล้วบันทึกลง EEPROM

```cpp
void setup() {
  setup_robot();
  get_maxMinA();  // กวาดหุ่นยนต์ไปมาให้ครบทุกสภาพ (ขาว-ดำ)
}
```

#### `get_maxMinB()` / `get_maxMinC()`
```cpp
get_maxMinB();  // Calibrate เซ็นเซอร์หลัง
get_maxMinC();  // Calibrate เซ็นเซอร์กลาง (Analog)
```

### โหลดค่า Calibration จาก EEPROM

```cpp
void setup() {
  setup_robot();  // <<< โหลดอัตโนมัติผ่าน get_EEP_Program()
}
```

หรือเรียกด้วยตัวเอง:
```cpp
read_eepA();  // โหลดค่า Sensor A
read_eepB();  // โหลดค่า Sensor B
read_eepC();  // โหลดค่า Sensor C
```

### ตรวจสอบค่า Calibration

```cpp
read_sensorA_program();  // แสดงค่า Max/Min ของ Sensor A
read_sensorB_program();
read_sensorC_program();
```

---

## ฟังก์ชันควบคุมมอเตอร์

### `Motor(left, right)`
ควบคุมมอเตอร์โดยตรง

```cpp
Motor(50, 50);    // เดินหน้าเร็ว 50%
Motor(-30, -30);  // ถอยหลังเร็ว 30%
Motor(50, 20);    // เลี้ยวขวาไปข้างหน้า
Motor(-50, 50);   // หมุนซ้ายติดที่
Motor(0, 0);      // หยุด
Motor(-1, -1);    // เบรกเบาๆ
```

**พารามิเตอร์:** -100 ถึง 100 (ติดลบ = ถอยหลัง)

### `set_Freq(motor_type)`
ตั้งค่าความถี่ PWM

```cpp
set_Freq("Coreless_Motors");  // 20kHz สำหรับมอเตอร์ Core-less
set_Freq("DC_Motors");        // 1kHz สำหรับมอเตอร์ DC ธรรมดา
```

---

## ฟังก์ชันเดินตามเส้น (Line Tracking)

### `fline()` - เดินหน้าตามเส้น
ฟังก์ชันหลักสำหรับเดินตามเส้นด้วย PID

#### รูปแบบที่ 1: เดินตามระยะทาง (cm)
```cpp
fline(speedL, speedR, kp, distance_cm, sensor_mode, stop_mode, power, target_sensor, brake_time);
```

**ตัวอย่าง:**
```cpp
// เดินหน้า 20cm ด้วย PID Kp=0.45 แล้วหยุด
fline(50, 50, 0.45, 20, 'f', 's', 50, "a6", 40);
```

#### รูปแบบที่ 2: เดินจนเจอแยก
```cpp
// เดินจนเจอแยกซ้าย (a0) แล้วหยุด
fline(50, 50, 0.45, "a0", 'f', 's', 50, "a6", 40);

// เดินจนเจอแยกขวา (a7) แล้วเลี้ยวขวา
fline(30, 30, 0.45, "a7", 'f', 'r', 80, "a5", 1);

// เดินจนเจอแยก 4 ทาง (a07) แล้วผ่าน
fline(50, 50, 0.45, "a07", 'f', 'p', 50, "a6", 0);
```

#### พารามิเตอร์

| พารามิเตอร์ | ความหมาย | ค่า |
|------------|----------|-----|
| `speedL, speedR` | ความเร็วซ้าย/ขวา | 0-100 |
| `kp` | ค่า Kp ของ PID | 0.1-2.0 (แนะนำ 0.45-0.85) |
| `distance` | ระยะทาง (cm) หรือ sensor string | `20` หรือ `"a0"`, `"a7"`, `"a07"` |
| `sensor_mode` | โหมดตรวจจับ | `'f'`=front, `'c'`=center, `'n'`=none |
| `stop_mode` | โหมดหยุด | `'s'`=stop, `'p'`=pass, `'l'`=left, `'r'`=right |
| `power` | กำลังเลี้ยว | 50-100 |
| `target_sensor` | เซ็นเซอร์เป้าหมาย | `"a0"` ถึง `"a7"` |
| `brake_time` | เวลาเบรก (ms) | 0-100 |

### `bline()` - ถอยหลังตามเส้น
ใช้งานเหมือน `fline()` แต่ถอยหลัง (ใช้ Sensor B)

```cpp
// ถอยหลัง 15cm แล้วหยุด
bline(50, 50, 0.45, 15, 'f', 's', 50, "b6", 30);

// ถอยหลังจนเจอแยกซ้าย
bline(30, 30, 0.45, "b0", 'f', 's', 50, "b5", 40);
```

### `fline4sensor()` - เดินตามเส้นด้วยเซ็นเซอร์ 4 ตัว
ใช้เซ็นเซอร์กลาง 4 ตัว (sensor 2-5) เหมาะกับความเร็วสูง

```cpp
// เดินหน้าจนเจอแยกซ้าย แล้วหยุด (ใช้ 4 เซ็นเซอร์)
fline4sensor(50, 50, 0.85, "a0", 'f', 's', 50, "a5", 1);

// เดินหน้าจนเจอแยก 4 ทาง แล้วผ่าน
fline4sensor(50, 50, 0.85, "a07", 'f', 'p', 50, "a5", 0);
```

### TracJC() - ฟังก์ชันย่อสำหรับเดินหาแยก

```cpp
TracJC();        // เดินจนเจอแยก 4 ทาง (a0 + a7)
TracJCL();       // เดินจนเจอแยกซ้าย (a0)
TracJCR();       // เดินจนเจอแยกขวา (a7)
TracJCC();       // เดินจนเจอเส้นกลาง (Sensor C)
TracJCC_P();     // เดินจนเจอเส้นกลางแล้วผ่าน
```

**ตัวอย่างการใช้งาน:**
```cpp
void loop() {
  TracJCL();   // เดินจนเจอแยกซ้าย
  TurnLeft();  // เลี้ยวซ้าย
  TracJCR();   // เดินจนเจอแยกขวา
  TurnRight(); // เลี้ยวขวา
}
```

### TracSpeedTime() - เดินตามเส้นตามเวลา

```cpp
// เดินตามเส้น 30 ความเร็ว นาน 2000ms พร้อมปรับหัวด้วย Gyro
TracSpeedTime(30, 2000, my);

// เดินตามเส้นและปรับหัวไปมุม 90 องศา
TracSpeedTime(30, 2000, my, 90.0);
```

---

## ฟังก์ชันควบคุมด้วย Gyro

### `fw_gyro()` - เดินหน้าตรงด้วย Gyro

```cpp
// เดินหน้าตรง 20cm ด้วย Gyro (Auto-align ด้วย)
fw_gyro(30, 30, 1.25, 12, 10);
//      ^   ^   ^    ^   ^
//      L   R   Kp   cm  brake
```

**การทำงาน:**
- เดินหน้าไปตามระยะทางที่กำหนด
- ควบคุมให้หัวหุ่นตรงตลอด (Yaw ≈ 0)
- Auto-align หลังหยุด

### `fw_gyro()` - เดินจนเจอเซ็นเซอร์

```cpp
// เดินหน้าตรงจนเจอเซ็นเซอร์ a0 แล้วหยุด
fw_gyro(20, 20, 0.55, 12, "a0", 's', 1);

// เดินหน้าตรงจนเจอเซ็นเซอร์ c0 แล้วผ่าน
fw_gyro(20, 20, 0.55, 10, "c0", 'p', 10);
```

### `bw_gyro()` - ถอยหลังตรงด้วย Gyro

```cpp
// ถอยหลังตรง 15cm
bw_gyro(30, 30, 1.25, 15, 10);

// ถอยหลังจนเจอเซ็นเซอร์ c01 (ทั้ง 2 ตัว)
bw_gyro(30, 30, 0.25, 20, "c01", 's', 1);
```

### `resetAngles()` - รีเซ็ตมุม Gyro

```cpp
resetAngles();           // รีเซ็ตมุมให้เป็น 0
my.resetAngles();       // เหมือนกัน
```

### `my.gyro(axis)` - อ่านค่ามุม

```cpp
float yaw = my.gyro('z');    // อ่านมุม Yaw (การหมุนในแนวนอน)
float pitch = my.gyro('y');  // อ่านมุม Pitch (เงยหน้า-ก้มหน้า)
float roll = my.gyro('x');   // อ่านมุม Roll (เอียงซ้าย-ขวา)
```

### `my.reCalibrateGyro()` - Calibrate Gyro

```cpp
my.reCalibrateGyro();  // ปรับ offset ใหม่ (~150ms)
```

**ลำดับการใช้งานที่แนะนำ:**
```cpp
void some_function() {
  my.resetAngles();       // รีเซ็ตมุม
  my.reCalibrateGyro();   // Calibrate
  my.resetAngles();       // รีเซ็ตอีกรอบ
  delay(50);
  
  // พร้อมใช้งาน
  float current_angle = my.gyro('z');
}
```

---

## ฟังก์ชันการเลี้ยว

### การเลี้ยวด้วย Gyro (หมุนตัว)

#### `rotate_right()` / `rotate_left()`
หมุนตัวด้วย PID + Gyro

```cpp
// หมุนขวา 90 องศา ด้วยกำลัง 40 และ offset 20
rotate_right(40, 90, 20);

// หมุนซ้าย 45 องศา
rotate_left(40, 45, 20);
```

### การเลี้ยวเพื่อวางของ (Place Functions)

สำหรับหมุนเพื่อวางของที่แยกซ้าย/ขวา

#### `place_left_in()` - หมุนซ้ายเข้าไปวางของ
```cpp
// หมุนซ้าย 45 องศา ด้วยกำลัง 30 และ offset 30
place_left_in(30, 45, 30);
```

#### `place_left_out()` - หมุนซ้ายออกมา
```cpp
place_left_out(30, 45, 30);
```

#### `place_right_in()` / `place_right_out()`
```cpp
place_right_in(30, 45, 30);   // หมุนขวาเข้าไป
place_right_out(30, 45, 30);  // หมุนขวาออกมา
```

**หมายเหตุ:** ฟังก์ชันเหล่านี้จะตรวจสอบ Gyro ก่อน หาก Gyro ใช้งานไม่ได้จะใช้ Encoder แทนอัตโนมัติ

---

## ฟังก์ชันแขนหุ่นยนต์และ Gripper

### ตั้งค่า Servo Gripper

```cpp
// ตั้งค่าองศาสำหรับ Gripper (เรียกใน setup)
int servo_down = 55;     // แขนลง
int servoL_open = 120;   // กางซ้าย
int servoR_open = 60;    // กางขวา
```

### ฟังก์ชันควบคุมแขนโดยตรง

#### `arm_down()` / `arm_up()`
```cpp
arm_down();       // แขนลง (smooth)
arm_up();         // แขนขึ้น (smooth)
arm_down(10);     // แขนลง (speed 10)
```

#### `gripper_open()` / `gripper_close()`
```cpp
gripper_open();   // กางมือ
gripper_close();  // หุบมือ
GO();             // กางมือ (ฟังก์ชันย่อ)
GC();             // หุบมือ (ฟังก์ชันย่อ)
```

### ควบคุมความสูงของแขน

#### `arm_height(level)`
ยกแขนตามระดับความสูง (0-6)

```cpp
arm_height(0);    // ลงสุด
arm_height(3);    // ความสูงกลาง
arm_height(6);    // ขึ้นสุด
```

**ตั้งค่าความสูงแต่ละระดับ:**
```cpp
int arm_height_pos[] = {163, 151, 150, 143, 142, 126, 70};
//                       ^    ^    ^    ^    ^    ^    ^
//                       0    1    2    3    4    5    6
```

### ฟังก์ชันย่อสำหรับแขน

#### Gripper Go Up (GGU)
ลงไปคีบแล้วยกขึ้น

```cpp
GGU();    // ลงไปคีบที่ระดับ 0 แล้วยกขึ้นระดับ 5
GGU1();   // ลงไปคีบที่ระดับ 1 แล้วยกขึ้นระดับ 5
GGU2();   // ลงไปคีบที่ระดับ 2 แล้วยกขึ้นระดับ 5
GGU3();   // ลงไปคีบที่ระดับ 3 แล้วยกขึ้นระดับ 5
GGU4();   // ลงไปคีบที่ระดับ 4 แล้วยกขึ้นระดับ 5
GGU5();   // ลงไปคีบที่ระดับ 5 แล้วยกขึ้นระดับ 6
```

**การทำงาน:**
```cpp
void GGU1() {
  arm_height(1);    // ลงไปที่ระดับ 1
  GC();             // หุบมือ (คีบ)
  delay(200);
  arm_height(5);    // ยกขึ้นระดับ 5
}
```

#### Gripper Drop Position (GDP)
ลงไปวางของแล้วกางมือ

```cpp
GDP();    // ลงวางระดับ 0 แล้วกางมือ
GDP1();   // ลงวางระดับ 1 แล้วกางมือ
GDP2();   // ลงวางระดับ 2 แล้วกางมือ
GDP3();   // ลงวางระดับ 3 แล้วกางมือ
GDP4();   // ลงวางระดับ 4 แล้วกางมือ
```

### ฟังก์ชัน Legacy (รูปแบบเก่า)

```cpp
arm_ready();       // แขนลง กางมือเตรียมคีบ
arm_open_down();   // กางมือ แล้วแขนลง
arm_down_open();   // แขนลง แล้วกางมือ
arm_open_up();     // กางมือ แล้วยกขึ้น
arm_up_open();     // ยกขึ้น แล้วกางมือ
arm_down_close();  // แขนลง แล้วหุบมือ
arm_up_close();    // ยกขึ้น แล้วหุบมือ
arm_big_box();     // โหมดคีบกล่องใหญ่
```

### ตั้งค่าและ Calibrate

#### `set_gripper_config()`
```cpp
// กำหนดองศาเปิด/ปิดของ Gripper
set_gripper_config(120, 75, 60, 75);
//                  ^    ^   ^   ^
//                  L_open L_close R_open R_close
```

#### `arm_init()`
เริ่มต้น Position ของ Servo

```cpp
void setup() {
  setup_robot();
  arm_init();  // ไปที่ตำแหน่งเริ่มต้น (down + open)
}
```

---

## ฟังก์ชัน Servo

### `servo(channel, angle)`
ควบคุม Servo โดยตรง

```cpp
servo(0, 90);    // Servo channel 0 -> 90 องศา
servo(1, 45);    // Servo channel 1 -> 45 องศา
servo(10, 120);  // Servo channel 10 -> 120 องศา
```

### ตั้งค่า Trim

```cpp
s0_trim(5);      // เพิ่ม offset ให้ Servo 0 +5 องศา
s1_trim(-3);     // ลด offset ให้ Servo 1 -3 องศา
s10_trim(10);    
s28_trim(0);
```

### Servo Smooth

```cpp
// เคลื่อนแบบ smooth (ทีละ 1 องศา)
servo_smooth(1, 90, 5);
//           ^  ^   ^
//           ch target speed(1-10)
```

---

## ตัวอย่างโค้ดจากไฟล์ต่างๆ

### ตัวอย่างจาก MyRP_Pico2_6sensor.ino

#### โครงสร้างพื้นฐาน

```cpp
#include <myrp_pico2.h>
#include "CONFIG.h"

int Speed, BaseSpeed;

void setup() {
  Speed = 50;
  BaseSpeed = Speed;
  setup_robot();
  set_motor();
  
  arm_down_open();  // เตรียมแขน
  
  sw();  // รอกดปุ่มเริ่ม
  
  // โปรแกรมหลัก
  Ball1();
  Ball2();
  Finish();
}

void loop() {
  // ไม่ใช้งานใน loop
}

void set_motor() {
  set_Freq("Coreless_Motors");
  distance_scale_fw(1.00);
  distance_scale_bw(1.10);
  set_kd_f(1.12);
  set_slow_motor(20, 20);
  set_turn_center_l(-90, 90);
  set_turn_center_r(90, -90);
  set_turn_front_l(-100, 100);
  set_turn_front_r(100, -100);
  set_brake_fc(5, 20);
  set_brake_bc(5, 20);
  set_delay_f(10);
}
```

### ตัวอย่างจาก TracJC.ino

#### เดินตามเส้นและหาแยก

```cpp
void example_trac() {
  // เดินจนเจอแยกซ้าย
  TracJCL(50, 0.85);
  
  // เดินจนเจอแยกขวา
  TracJCR(50, 0.85);
  
  // เดินจนเจอแยก 4 ทาง
  TracJC(50, 0.85);
  
  // เดินจนเจอเส้นกลาง (Sensor C)
  TracJCC(30, 0.45);
}
```

#### เดินตามเวลาพร้อม Auto-align

```cpp
void example_trac_time() {
  // เดินตามเส้น 30 ความเร็ว เป็นเวลา 2 วินาที
  TracSpeedTime(30, 2000, my);
  
  // เดินตามเส้นและปรับหัวไปมุม 90 องศา
  my.resetAngles();
  TracSpeedTime(30, 2000, my, 90.0);
}
```

### ตัวอย่างจาก Arm.ino

#### ลำดับการคีบและวางของ

```cpp
void pickup_and_place_sequence() {
  // เตรียมแขน
  arm_down_open();
  delay(500);
  
  // เข้าไปคีบของที่ระดับ 2
  arm_height(2);
  delay(300);
  GC();  // หุบมือ
  delay(200);
  
  // ยกขึ้น
  arm_height(5);
  delay(300);
  
  // ไปวางที่ระดับ 1
  arm_height(1, 20);
  delay(300);
  GO();  // กางมือ
  
  // กลับที่เดิม
  arm_height(5);
}
```

#### ใช้ฟังก์ชันย่อ

```cpp
void quick_pickup() {
  sw();
  GGU();    // คีบที่ระดับ 0 ยกขึ้น 5
  
  sw();
  GGU1();   // คีบที่ระดับ 1 ยกขึ้น 5
  
  sw();
  GGU2();   // คีบที่ระดับ 2 ยกขึ้น 5
}

void quick_drop() {
  GDP();    // วางลงระดับ 0
  GDP1();   // วางลงระดับ 1
  GDP2();   // วางลงระดับ 2
}
```

### ตัวอย่างจาก ReadMe.ino

#### ตัวอย่างเดินตามเส้นและเลี้ยว

```cpp
void example_line_and_turn() {
  // เดินหน้า 20cm
  fline(50, 50, 0.45, 20, 'f', 's', 50, "a6", 40);
  
  // เดินจนเจอแยกซ้าย แล้วเลี้ยวซ้าย
  fline(30, 30, 0.45, "a0", 'f', 'l', 80, "a2", 1);
  
  // เดินจนเจอแยกขวา แล้วเลี้ยวขวา
  fline(30, 30, 0.45, "a7", 'f', 'r', 80, "a5", 1);
  
  // เดินจนเจอเส้นกลาง (Sensor C) แล้วหยุด
  fline(30, 30, 0.45, 0, 'c', 's', 50, "a6", 40);
  
  // เดินจนเจอแยก 4 ทาง แล้วผ่าน
  fline(50, 50, 0.45, "a07", 'f', 'p', 50, "a6", 0);
}
```

#### ตัวอย่างถอยหลัง

```cpp
void example_backward() {
  // ถอยหลัง 15cm
  bline(50, 50, 0.45, 15, 'f', 's', 50, "b6", 30);
  
  // ถอยหลังจนเจอแยกและเลี้ยว
  bline(30, 30, 0.45, "b0", 'f', 'l', 50, "b2", 40);
}
```

#### ตัวอย่างใช้ Gyro

```cpp
void example_gyro() {
  // รีเซ็ต Gyro
  my.resetAngles();
  my.reCalibrateGyro();
  my.resetAngles();
  
  // เดินหน้าตรง 25cm
  fw_gyro(30, 30, 1.25, 25, 10);
  
  // ถอยหลังตรง 20cm
  bw_gyro(30, 30, 1.25, 20, 10);
  
  // เดินจนเจอเซ็นเซอร์ a0 (ด้วย Gyro)
  fw_gyro(30, 30, 1.25, 10, "a0", 's', 10);
}
```

### ตัวอย่างจาก CONFIG.h

#### ตัวแปร Config ที่ใช้ทั่วโปรแกรม

```cpp
// PID Variables (จาก Initial.ino)
extern float Kp, Ki, Kd, Kt;       // PID เดินหน้า
extern float KpB, KiB, KdB, KtB;   // PID ถอยหลัง

// Speed Settings (จาก TracJC.ino)
extern int TRAC_SPEED;  // default: 50
extern int TRAC_SLOW;   // default: 30
extern int TRAC_FAST;   // default: 70

// Turn Settings (จาก Turn.ino)
extern int TURN_Speed;       // default: 40
extern int TURN_BrakeSpeed;  // default: 100
extern int TURN_BrakeTime;   // default: 30

// Servo Positions
#define SERVO_DOWN   85
#define SERVO_UP     170
#define SERVOL_OPEN  0
#define SERVOL_CLOSE 75
```

---

## เทคนิคและคำแนะนำการใช้งาน

### การปรับค่า PID

```cpp
// Kp ต่ำ (0.3-0.6) = นุ่มนวล, เหมาะกับความเร็วต่ำ
fline(30, 30, 0.45, 20, 'f', 's', 50, "a6", 40);

// Kp กลาง (0.7-1.2) = สมดุล, เหมาะกับความเร็วปานกลาง
fline(50, 50, 0.85, 20, 'f', 's', 50, "a6", 40);

// Kp สูง (1.3-2.0) = คมชัด, เหมาะกับความเร็วสูง
fline(70, 70, 1.5, 20, 'f', 's', 50, "a6", 40);
```

### การใช้ Gyro อย่างถูกต้อง

```cpp
void proper_gyro_usage() {
  // 1. Reset ก่อนใช้เสมอ
  my.resetAngles();
  my.reCalibrateGyro();
  my.resetAngles();
  delay(50);
  
  // 2. ใช้งาน
  fw_gyro(30, 30, 1.25, 15, 10);
  
  // 3. ถ้าต้องหมุน reset อีกครั้ง
  my.resetAngles();
  rotate_right(40, 90, 20);
}
```

### การประหยัดเวลา Calibrate

```cpp
void setup() {
  setup_robot();  // โหลดค่า Calibration จาก EEPROM อัตโนมัติ
  set_motor();
  
  // ไม่ต้อง calibrate ทุกครั้ง หากค่าเดิมยังใช้ได้
  // ถ้าต้องการ calibrate ใหม่ ให้กดปุ่มใน sw()
  sw();
}
```

### เทคนิคลดการสั่น

#### 1. ใช้ `fline4sensor()` แทน `fline()`
```cpp
// เซ็นเซอร์กลาง 4 ตัวทำให้เสถียรกว่า
fline4sensor(50, 50, 0.85, "a0", 'f', 's', 50, "a5", 1);
```

#### 2. ลด Kd ถ้าสั่นมาก
```cpp
set_kd_f(0.8);  // ลดจาก 1.12 เหลือ 0.8
```

#### 3. เพิ่มเวลาเบรก
```cpp
// เพิ่มเวลาเบรกให้นานขึ้น
fline(50, 50, 0.85, 20, 'f', 's', 50, "a6", 60);
//                                            ^^ เพิ่มจาก 40 เป็น 60
```

---

## ตารางสรุปฟังก์ชันทั้งหมด

### Sensor Functions
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `read_sensorA(n)` | อ่านเซ็นเซอร์หน้า 0-7 |
| `read_sensorB(n)` | อ่านเซ็นเซอร์หลัง 0-7 |
| `md_sensorA(n)` | ค่ากลางเซ็นเซอร์ A |
| `get_maxMinA()` | Calibrate เซ็นเซอร์ A |
| `read_eepA()` | โหลดค่า Calibration A |

### Motor Control
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `Motor(L, R)` | ควบคุมมอเตอร์ -100 ถึง 100 |
| `set_Freq(type)` | ตั้งค่าความถี่ PWM |

### Line Tracking
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `fline()` | เดินหน้าตามเส้น |
| `bline()` | ถอยหลังตามเส้น |
| `fline4sensor()` | เดินด้วยเซ็นเซอร์ 4 ตัว |
| `TracJC()` | เดินหาแยก 4 ทาง |
| `TracJCL()` | เดินหาแยกซ้าย |
| `TracJCR()` | เดินหาแยกขวา |
| `TracJCC()` | เดินหาเส้นกลาง |
| `TracSpeedTime()` | เดินตามเวลา |

### Gyro Control
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `fw_gyro()` | เดินหน้าตรงด้วย Gyro |
| `bw_gyro()` | ถอยหลังตรงด้วย Gyro |
| `rotate_right()` | หมุนขวาด้วย Gyro |
| `rotate_left()` | หมุนซ้ายด้วย Gyro |
| `resetAngles()` | รีเซ็ตมุม Gyro |
| `my.gyro('z')` | อ่านมุม Yaw |
| `my.reCalibrateGyro()` | Calibrate Gyro |

### Arm & Gripper
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `arm_down()` | แขนลง |
| `arm_up()` | แขนขึ้น |
| `arm_height(n)` | ตั้งความสูง 0-6 |
| `gripper_open()` | กางมือ |
| `gripper_close()` | หุบมือ |
| `GO()` | กางมือ (ฟังก์ชันย่อ) |
| `GC()` | หุบมือ (ฟังก์ชันย่อ) |
| `GGU()` - `GGU5()` | คีบแล้วยกขึ้น |
| `GDP()` - `GDP4()` | ลงไปวางของ |
| `arm_ready()` | เตรียมคีบ |

### Servo Control
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `servo(ch, angle)` | ควบคุม Servo |
| `servo_smooth(ch, angle, spd)` | Servo แบบ smooth |
| `s0_trim(offset)` | ตั้งค่า trim Servo 0 |

### Utility
| ฟังก์ชัน | คำอธิบาย |
|---------|----------|
| `setup_robot()` | เริ่มต้นระบบ |
| `sw()` | รอกดปุ่ม/Calibrate |
| `waitButton()` | รอกดปุ่ม 3 |
| `tone(freq, duration)` | เล่นเสียง Buzzer |
| `blinkLED(count)` | กระพริบ LED |

---

## สรุป

Library **MyRP_Pico2** รวบรวมฟังก์ชันครบครันสำหรับการพัฒนาหุ่นยนต์แข่งขันระดับสูง:

✅ **เซ็นเซอร์**: รองรับสูงสุด 18 เซ็นเซอร์ (8+8+2)  
✅ **PID**: ปรับแต่งได้ละเอียด รองรับทั้งเดินหน้า/ถอยหลัง  
✅ **Gyro**: เดินตรง/หมุนตัวแม่นยำ  
✅ **แขนกล**: ควบคุมแบบ Smooth พร้อมฟังก์ชันย่อ  
✅ **EEPROM**: บันทึก Calibration ไม่ต้องปรับค่าทุกครั้ง  

**เริ่มต้นใช้งาน:**
1. Include `<myrp_pico2.h>`
2. เรียก `setup_robot()` ใน setup()
3. ตั้งค่ามอเตอร์ด้วย `set_motor()`
4. เรียก `sw()` เพื่อรอเริ่ม
5. ใช้ฟังก์ชันตามต้องการ

**ติดต่อสอบถาม:** MyRP Team  
**GitHub:** [Repository URL]  
**Version:** 1.0.0  
**License:** MIT

---

*เอกสารนี้ครอบคลุมฟังก์ชันทั้งหมดใน myrp_pico2.h พร้อมตัวอย่างการใช้งานจากไฟล์ MyRP_Pico2_6sensor.ino, TracJC.ino, Arm.ino, ReadMe.ino และ CONFIG.h*
